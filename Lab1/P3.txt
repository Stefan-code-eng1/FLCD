#adapt *basic*

f type INT as Sum(vec)
{
      create(sum) type INT
      for( i eq 0; i less Length(vec); i eq i plus 1)
            sum eq sum plus vec of i
      give sum
}

f type INT as power()
{{
     create(a) type INT with 100
     read(a)
     write Sum(a) type STRING
     give 0
}} 